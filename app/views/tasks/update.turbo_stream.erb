<%= turbo_stream.remove "edit_task_#{@task.id}" %>
<%= turbo_stream.replace "status_bar", partial: "tasks/progress_bar", locals: { completed_width_percent: @completed_width_percent, remaining_width_percent: @remaining_width_percent } %>